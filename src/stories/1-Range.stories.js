import React from "react";
import Range from "../components/Range";

export default {
  title: "Range",
  component: Range,
};

export const Simple = () => (
 
    <div style={{ width: "300px" }}>
      <Range />
    </div>

);